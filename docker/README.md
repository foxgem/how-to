# Docker的基本使用
docker的基本使用参考官方文档。学习docker的入门可以从`Dockerfile`开始，本仓库存放了一些`Dockerfile`的范例和`docker-compose`的范例，可供参考。

build一个docker镜像:

    cd dir/contains/Dockerfile
    docker build .

运行docker-compose范例:

    cd dir/contains/docker-compose.yml
    docker-compose up
